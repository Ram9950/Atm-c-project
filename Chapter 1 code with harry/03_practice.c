#include <stdio.h>
void main()
{
    int radius = 3;
    float pi = 3.14;
    int height = 3;

    printf("area of circle is %f \n ", pi * radius * radius);

    printf("volume of cylinder is %f", pi * radius * radius * height);
}