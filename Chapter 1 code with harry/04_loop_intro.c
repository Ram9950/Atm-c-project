#include <stdio.h>
void main()
{
    printf("Hello");
    printf("world \n");
    int a=1;
    // Loops are used to repeat similiar part of code snippet effficiently.
    // when we have to write numbers from 1 to 100 so if we repeat 
    // this phenonmna by taking a++ many times which is very diffficult so 
    // we use loops as "while loop", "for while " loop, "do while" loop etc
